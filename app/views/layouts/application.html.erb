<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    "application", media: "all", 
  "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>

</head>
<body>
	<%= render 'layouts/header' %>
	
	<div class="container">
    #a message that appears on the subsequent page (in this case, welcoming our new user to the #application) and then disappears upon visiting a second page or on page reload
    # see app/controllers/users_controller.rb  to make your own flash message
    <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>

		<%= yield %>
        <%= render 'layouts/footer' %>
        #this line on the bottom just shows some useful infos about your webpages
        <%= debug(params) if Rails.env.development? %> 

	</div>
</body>
</html>
